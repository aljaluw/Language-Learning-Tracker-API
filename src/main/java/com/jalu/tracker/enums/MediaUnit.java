package com.jalu.tracker.enums;

public enum MediaUnit {
    CHARACTERS,  // Used for READING and VISUAL_NOVEL
    PAGES,       // Alternative for READING (books, manga)
    CHAPTERS,    // Alternative for READING (web novels, light novels)
    EPISODES,    // Used for TV_SHOW
    MINUTES;     // Used for MOVIE, GAME, LISTENING
}
